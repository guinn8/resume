
% This is a latex template, PRESERVE THIS COMMENT `pandoc resume.md -o resume.pdf --pdf-engine=xelatex`

\documentclass[10pt,a4paper,sans]{moderncv}

% moderncv themes
\moderncvstyle[shortrules]{banking}  % Style options: 'casual' (default), 'classic', 'oldstyle' and 'banking'
\moderncvcolor{blue}     % Color options: 'blue' (default), 'orange', 'green', 'red', 'purple', 'grey' and 'black'
\usepackage[scale=0.9]{geometry} % Reduced margins

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}} % Tighten lists

\renewcommand*{\namefont}{\fontsize{14}{16}\selectfont} % Change name font size


\makeatletter
\newlength{\baseletterheight}
\setlength{\baseletterheight}{1ex} % Adjust as needed
\makeatother

\usepackage{etoolbox}

\BeforeBeginEnvironment{verbatim}{\vspace{-1ex}}  % Reduce space before the code block
\AfterEndEnvironment{verbatim}{\vspace{-1ex}}     % Reduce space after the code block


\makeatletter

\RenewDocumentCommand{\subsection}{sm}{%
  \par\addvspace{0.75ex}%
  \phantomsection{}%
  \addcontentsline{toc}{subsection}{#2}%
  \if@left\else\if@fullrules\else%
    \subsectionrule\fi\fi%
  \strut\subsectionstyle{#2}%
  \if@fullrules%
    \subsectionrule%
  \else\if@right\else%
    \subsectionrule\fi\fi%
  \par\nobreak\addvspace{0.5ex}\@afterheading}

\RenewDocumentCommand{\section}{sm}{%
  \par\addvspace{1.5ex}%
  \phantomsection{}% reset the anchor for hyperrefs
  \addcontentsline{toc}{section}{#2}%
  \if@left\else\if@fullrules\else\if@mixedrules\else%
    \sectionrule\fi\fi\fi%
  \strut\sectionstyle{#2}%
  \if@fullrules%
    \sectionrule%
  \else\if@mixedrules%
    \sectionrule%
  \else\if@right\else%
    \sectionrule\fi\fi\fi%
  \par\nobreak\addvspace{0.5ex}\@afterheading}
\makeatother

\name{${name} \textcolor{gray}{ -- \textit{${address}}}}{}
\phone[mobile]{${phone}}
\email{${email}}
\social[github]{${github}}
\social[linkedin]{${linkedin}}

\begin{document}

\makecvtitle
\vspace*{-2em}

$body$

\end{document}
